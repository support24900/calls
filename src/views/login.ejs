<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mirai Skin — Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center">
  <div class="bg-white p-8 rounded-lg shadow-md w-full max-w-sm">
    <h1 class="text-2xl font-bold text-center text-pink-600 mb-6">🌸 Mirai Skin</h1>
    <% if (error) { %>
      <div class="bg-red-50 text-red-600 p-3 rounded mb-4 text-sm"><%= error %></div>
    <% } %>
    <form method="POST" action="/dashboard/login">
      <label class="block text-sm font-medium text-gray-700 mb-2">Dashboard Password</label>
      <input type="password" name="password" class="w-full border rounded-lg px-3 py-2 mb-4 focus:ring-2 focus:ring-pink-300 outline-none" required autofocus>
      <button type="submit" class="w-full bg-pink-600 text-white py-2 rounded-lg hover:bg-pink-700 font-medium">Login</button>
    </form>
  </div>
</body>
</html>
